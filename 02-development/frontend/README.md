# Flux frontend

Vite + React + TypeScript UI for the Flux Mini Kanban Board.

## Run locally

```bash
npm install
npm run dev
```

Task CRUD currently goes through `src/services/taskService.ts` (in-memory mock). Replace that module with API calls when the FastAPI backend is ready.
